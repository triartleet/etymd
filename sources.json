{
  "$comment": "Corpus manifest — the sibling repos etymd validates against (the dogfood harness). An entry marked `private` is named by SHAPE, not by directory: its local directory comes from an untracked sources.local.json (see README). Corpus tests skip cleanly wherever a repo is absent.",
  "sources": [
    {
      "name": "pepshop",
      "path": "../pepshop",
      "role": "primary",
      "shape": "pnpm-workspace / TanStack Start + Express/tRPC",
      "note": "frontrunner — the locked workflow and golden fixture for corpus reproduction"
    },
    {
      "name": "nx-monorepo",
      "private": true,
      "role": "reference",
      "shape": "Nx monorepo / Next.js",
      "note": "monorepo topology coverage: meta-vs-narrow command classification, a reuse-first inventory doc, and an advisory AI-review CI job"
    },
    {
      "name": "spa-bff",
      "private": true,
      "role": "reference",
      "shape": "single-repo React SPA + Express BFF",
      "note": "non-monorepo client/server split coverage; Copilot-instructions dialect"
    },
    {
      "name": "cra-legacy",
      "private": true,
      "role": "reference",
      "shape": "CRA + Apollo + husky v3/lint-staged",
      "note": "legacy command-set and hook-setup coverage; the script-less coverage job that motivated the gate-integrity honesty rules"
    },
    {
      "name": "cc-gg-bridgy",
      "path": "../cc-gg-bridgy",
      "role": "reference",
      "shape": "single-repo VS Code extension / esbuild + pnpm",
      "note": "smallest surface; first etymd-scaffolded contract in daily use (init dogfood 2026-07-26)"
    },
    {
      "name": "wonderbee",
      "path": "../wonderbee",
      "role": "reference",
      "shape": "docs-only, no package manifest",
      "note": "pre-design repo; covers the no-manifest scaffold class (the see-package.json fix)"
    },
    {
      "name": "wonder-worker",
      "path": "../wonder-worker",
      "role": "reference",
      "shape": "pnpm + Bun fork of an active upstream OSS assistant",
      "note": "large claim-rich surface: 29KB CLAUDE.md, symlinked+gitignored AGENTS.md, husky; the fork-scoping case"
    }
  ]
}
